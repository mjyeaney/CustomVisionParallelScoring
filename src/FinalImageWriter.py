import logging
from PIL import Image

class FinalImageWriter:
    def WriteFinalImage(self, originalSource, boundingBoxInfo):
        # Use PIL.ImageDraw.ImageDraw.rectangle(xy, fill=None, outline=None, width=0) to overlay boxes
        pass